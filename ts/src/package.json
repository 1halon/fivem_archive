{
  "scripts": {
    "build": "pnpm run clean && npx tsc && pnpm run export && pnpm run copy:manifest && pnpm run manifest",
    "clean": "rimraf dist/ .turbo/",
    "copy:manifest": "bash bash/copy:manifest",
    "export": "bash bash/export",
    "manifest": "bash bash/manifest"
  },
  "devDependencies": {
    "configs": "workspace:*"
  }
}
