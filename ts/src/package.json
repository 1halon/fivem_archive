{
  "dependencies": {
    "configs": "workspace:*"
  },
  "scripts": {
    "build": "pnpm run clean && npx tsc && npx webpack && pnpm run export && pnpm run copy:manifest && pnpm run manifest && pnpm run copy:files",
    "clean": "rimraf dist/ .turbo/",
    "copy:files": "bash bash/copy:files",
    "copy:manifest": "bash bash/copy:manifest",
    "export": "bash bash/export",
    "manifest": "bash bash/manifest"
  }
}
