{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "cache": false,
      "dependsOn": ["^build"],
      "inputs": ["src/*/**.ts"],
      "outputs": ["dist/*/**.js"]
    }
  }
}
